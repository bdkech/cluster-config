apiVersion: v1
kind: ConfigMap
metadata:
  name: config-domain
  namespace: knative-serving
  labels:
    app.kubernetes.io/name: knative-serving
    app.kubernetes.io/component: controller
data:
  # Configure default domain for Knative services
  # Services will be accessible at: <service>.<namespace>.scudco.com
  scudco.com: ""
  # Remove the default svc.cluster.local domain
  _example: |
    ################################
    #                              #
    #    EXAMPLE CONFIGURATION     #
    #                              #
    ################################